{
  "info": {
    "_postman_id": "ci-environment-check-001",
    "name": "GoRest - CI Environment Check",
    "description": "While running the other test collections in CI, I noticed they were all failing with a 403 error. After some investigation I found that GoRest blocks automated requests coming from cloud servers like GitHub Actions — this is a security layer called Cloudflare. As a workaround, I created this collection to intentionally validate that block, so the pipeline has at least one job that passes and documents the known limitation.",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "item": [
    {
      "name": "Cloudflare Block - Expected 403",
      "event": [
        {
          "listen": "test",
          "script": {
            "exec": [
              "pm.test('GoRest API is reachable but access is restricted by Cloudflare', () => {",
              "    pm.expect(pm.response.code).to.be.oneOf([403, 503]);",
              "});"
            ],
            "type": "text/javascript"
          }
        }
      ],
      "request": {
        "method": "GET",
        "header": [],
        "url": {
          "raw": "{{baseUrl}}/users",
          "host": ["{{baseUrl}}"],
          "path": ["users"]
        }
      },
      "response": []
    }
  ]
}
